<!--pages/apply_list_1/apply_list.wxml-->
<!--导航条-->
<view class="navbar">
  <text wx:for="{{navbar}}" data-idx="{{index}}" class="item {{currentTab==index ? 'active' : ''}}" wx:key="unique" bindtap="navbarTap">{{item}}</text>
</view>
<!-- 已授权 -->
<view hidden="{{currentTab!==0}}">
  <view class="container">
  <block wx:for-items="{{client}}">
    <view wx:if="{{item.done}}">
      <view class="client_info">
        <image src="../../images/coin.png " class="userinfo-avatar " background-size="cover "></image>
        <view class="name ">{{item.name}}</view>
        <view class="btn_accept_hover ">已接受</view>
      </view>
    </view>
  </block>
  </view>
</view>



<!-- 申请 -->
<view hidden="{{currentTab!==1}}">
  <view class="container">
    <block wx:for-items="{{client}}">
      <view wx:if="!{{item.done}}" class="client_info">
        <image src="../../images/coin.png " class="userinfo-avatar " background-size="cover"></image>
        <view class="name ">{{item.name}}</view>
        <view class="btn_accept " hover="true " hover-class="btn_accept_hover " bindtap="agree">接受</view>
      </view>
  </block>
</view>
</view>